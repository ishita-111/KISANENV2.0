# docker-compose.yml
version: "3.9"

services:
  kisanenv:
    build: .
    ports:
      - "8000:8000"
    environment:
      - KISANENV_LLM_BACKEND=mock
    volumes:
      - .:/app               # Mount code for hot-reload
      - ./checkpoints:/app/checkpoints
    command: uvicorn run:app --host 0.0.0.0 --port 8000 --reload
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
